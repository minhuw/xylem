events {
    worker_connections 1024;
}

http {
    access_log /dev/stdout;
    error_log /dev/stderr;

    server {
        listen 80;

        location / {
            return 200 "OK\n";
            add_header Content-Type text/plain;
        }
    }
}
